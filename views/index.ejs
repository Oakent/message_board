<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% messages.forEach( message=>{%>
    <p><%=message.text%> from <%= message.user%> at <%=message.added %></p>
    <% })%>

    <div>
      <a href="/new">send your own message!</a>
    </div>
  </body>
</html>
